#include "main.h"
"include <stdio.h>
/**
*_isupper - function to print letters if upper case
*@c: argument taking
*Return: 1 or 0
*/

int _isupper(int c)
{
	if (c => 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
