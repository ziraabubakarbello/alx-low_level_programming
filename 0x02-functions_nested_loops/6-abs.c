#include "main.h"

/**
 * _abs - function that prints the absolute int
 * @n: absolute integer
 * Return: absolute value
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
